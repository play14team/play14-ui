query Markers {
  events(
    pagination: { limit: 10000 }
    filters: { status: { ne: "Cancelled" }, venue: { location: { ne: {} } } }
  ) {
    data {
      id
      attributes {
        slug
        name
        start
        end
        status
        venue {
          data {
            attributes {
              name
              address
              location
            }
          }
        }
      }
    }
  }
}
