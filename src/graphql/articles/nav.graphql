query ArticleNav {
  articles(sort: "publishedAt:desc", pagination: { limit: 5000 }) {
    slug
    title
    category
    tags {
      value
    }
    publishedAt
    defaultImage {
      name
      url
      width
      height
    }
  }
}
