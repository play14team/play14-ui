query EventNav {
  events(sort: "start:desc", pagination: { limit: 10000 }) {
    data {
      attributes {
        slug
        name
        start
        defaultImage {
          data {
            id
            attributes {
              name
              url
              blurhash
              width
              height
            }
          }
        }
      }
    }
  }
}
