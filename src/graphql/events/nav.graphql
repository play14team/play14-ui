query EventNav {
  events(sort: "start:desc", pagination: { limit: 5000 }) {
    slug
    name
    start
    status
    location {
      slug
      name
      country
    }
    defaultImage {
      name
      url
      width
      height
    }
  }
}
