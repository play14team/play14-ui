query EventNav {
  events(sort: "start:desc", pagination: { limit: 5000 }) {
    slug
    name
    start
    eventStatus
    location {
      slug
      name
      country
    }
    defaultImage {
      name
      url
      width
      height
    }
  }
}
