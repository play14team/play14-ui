query GameNav {
  games(sort: "name:asc", pagination: { limit: 5000 }) {
    slug
    name
    category
    tags {
      value
    }
    publishedAt
    defaultImage {
      name
      url
      width
      height
    }
  }
}
